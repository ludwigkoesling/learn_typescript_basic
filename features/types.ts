// easy way (shortcut) to refer to the properties and functions that value has 
// in addition every value has a type 

const today = new Date();
today.getTime();

const person = {
    age: 20
};

class Color {}
const red = new Color(); 


